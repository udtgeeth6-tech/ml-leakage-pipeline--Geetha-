# ml-leakage-pipeline--Geetha-
 Your team has been asked to evaluate a classification model on a structured dataset and ensure the evaluation workflow is free from data leakage. You will compare a flawed baseline approach against a corrected pipeline-based approach, and use cross-validation to assess model reliability.
Experiment with Decision Tree Depth Train a DecisionTreeClassifier at max_depth values of 1, 5, and 20 using the same train-test split from Task 2. Record train and test accuracy for each depth in a table and briefly explain which depth best balances fit and generalization.

